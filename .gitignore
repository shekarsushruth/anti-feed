# personal planning doc — not for the public repo
anti-feed-build-brief.md

# local drafts the agent may write before finalizing
draft.json
*.draft.json

# secrets — never commit (GitHub push token for the daily job)
.env
.env.*
*.token

# editor / OS noise
.DS_Store
Thumbs.db
node_modules/
